#include <Command.hpp>

/**
 * @note USER MODE (not necessary)
 * @brief changes which affect either how the client is seen by others or what 'extra'
 * message the client is
 *
 * MODE <nickname> *(( + / - ))*( i / w / o / 0 / r ))
 */
void MODE(Client &client, const std::vector<std::string> &params) {

}
